GeoHome — Landing

This branch contains a single static landing page with a Leaflet map and simulated properties.

Files:
- index.html — static landing page

To preview locally, open `index.html` in a browser.
